[project]
name = "report"
version = "0.0.1"
description = "yobibyte's reports library"
readme = "README.md"
dependencies = [
  "matplotlib",
  "numpy",
  "jinja2",
]

[project.scripts]
report = "report.base:main"
